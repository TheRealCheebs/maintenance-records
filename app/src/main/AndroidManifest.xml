<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MaintenanceRecordsApp"
        android:allowBackup="false"
        android:networkSecurityConfig="@xml/network_security_config"
        android:preserveLegacyExternalStorage="false"
        android:requestLegacyExternalStorage="false"
        android:icon="@android:mipmap/sym_def_app_icon"
        android:roundIcon="@android:mipmap/sym_def_app_icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.MaintenanceRecords"
        tools:targetApi="31">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.MaintenanceRecords">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.CreateRecordActivity"
            android:label="Create Record"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".ui.RecordDetailActivity"
            android:label="Record Details"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".ui.NostrVerifyActivity"
            android:label="Nostr Verification"
            android:parentActivityName=".RecordDetailActivity" />
    </application>
</manifest>
